export const POINCARE_WIDTH = Math.min(window.innerHeight, window.innerWidth) * 0.7;
export const POINCARE_HEIGHT = Math.min(window.innerHeight, window.innerWidth) * 0.7;

export const POINT_RADIUS = 3;
export const POINT_COLOR = 'black';

export const LINE_WIDTH = 2;
export const LINE_COLOR = 'gray';

export const DEFAULT_P = 5;
export const DEFAULT_Q = 8;